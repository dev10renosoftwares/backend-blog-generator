# AI Blog Generator – API Contract

## 1. Authentication APIs

### 1.1 Register User

**Endpoint:**
`POST /api/auth/register`

**Authorization:** Public

**Request**

```json
{
  "userName": "johnsmith",
  "email": "john@example.com",
  "password": "Password@123"
}
```

**Success Response — 201 Created**

```json
{
  "success": true,
  "message": "User registered successfully.",
  "data": {
    "accessToken": "",
    "refreshToken": "",
    "expiresIn": 3600,
    "user": {
      "userId": 1,
      "userName": "johnsmith",
      "email": "john@example.com",
      "role": "User",
      "availableCredits": 100,
      "profilePictureUrl": null
    }
  }
}
```

**Error Responses**

**400 Bad Request**

```json
{
  "success": false,
  "message": "Validation failed.",
  "errors": {
    "userName": ["Username is required."],
    "email": ["A valid email address is required."],
    "password": [
      "Password must contain at least 8 characters including uppercase, lowercase, number and special character."
    ]
  }
}
```

**409 Conflict**

```json
{
  "success": false,
  "message": "Username already exists."
}
```

**409 Conflict**

```json
{
  "success": false,
  "message": "Email is already registered."
}
```

---

### 1.2 Login

**Endpoint:**
`POST /api/auth/login`

**Authorization:** Public

**Request**

```json
{
  "email": "john@example.com",
  "password": "Password@123"
}
```

**Success Response — 200 OK**

```json
{
  "success": true,
  "message": "Login successful.",
  "data": {
    "accessToken": "",
    "refreshToken": "",
    "expiresIn": 3600,
    "user": {
      "userId": 1,
      "userName": "johnsmith",
      "email": "john@example.com",
      "role": "User",
      "availableCredits": 95,
      "profilePictureUrl": "https://cdn.aiblog.com/profile/1.jpg"
    }
  }
}
```

**401 Unauthorized**

```json
{
  "success": false,
  "message": "Invalid email or password."
}
```

**403 Forbidden**

```json
{
  "success": false,
  "message": "Your account has been suspended."
}
```

---

### 1.3 Refresh Token

**Endpoint:**
`POST /api/auth/refresh-token`

**Authorization:** Public

**Request**

```json
{
  "refreshToken": ""
}
```

**Success Response — 200 OK**

```json
{
  "success": true,
  "message": "Token refreshed successfully.",
  "data": {
    "accessToken": "",
    "refreshToken": "",
    "expiresIn": 3600
  }
}
```

**401 Unauthorized**

```json
{
  "success": false,
  "message": "Refresh token is invalid or expired."
}
```

---

### 1.4 Logout

**Endpoint:**
`POST /api/auth/logout`

**Authorization:** User

**Request**

```json
{
  "refreshToken": ""
}
```

**Success Response — 200 OK**

```json
{
  "success": true,
  "message": "Logged out successfully."
}
```

---

# 2. User Profile APIs

### 2.1 Get My Profile

**Endpoint:** `GET /api/profile`
**Authorization:** User

**Success Response — 200 OK**

```json
{
  "success": true,
  "message": "Profile retrieved successfully.",
  "data": {
    "userId": 1,
    "userName": "johnsmith",
    "email": "john@example.com",
    "displayName": "John Smith",
    "bio": "Backend Developer | AI Enthusiast",
    "location": "New York",
    "website": "https://johnsmith.dev",
    "profilePictureUrl": "https://cdn.aiblog.com/profile/1.jpg",
    "availableCredits": 95,
    "role": "User",
    "followersCount": 15,
    "followingCount": 8,
    "publishedBlogs": 12,
    "createdAt": "2026-08-01T10:30:00Z"
  }
}
```

---

### 2.2 Update Profile

**Endpoint:** `PUT /api/profile`
**Authorization:** User

**Request**

```json
{
  "displayName": "John Smith",
  "bio": "Backend Developer | AI Enthusiast",
  "location": "New York",
  "website": "https://johnsmith.dev"
}
```

**Success Response — 200 OK**

```json
{
  "success": true,
  "message": "Profile updated successfully."
}
```

---

### 2.3 Change Password

**Endpoint:** `PUT /api/profile/change-password`
**Authorization:** User

**Request**

```json
{
  "oldPassword": "OldPassword@123",
  "newPassword": "NewPassword@123",
  "confirmNewPassword": "NewPassword@123"
}
```

**Success Response — 200 OK**

```json
{
  "success": true,
  "message": "Password changed successfully."
}
```

**400 Bad Request**

```json
{
  "success": false,
  "message": "Old password is incorrect."
}
```

---

### 2.4 Upload Profile Picture

**Endpoint:** `POST /api/profile/upload-picture`
**Authorization:** User

**Request:** `multipart/form-data`

| Key            | Type |
| -------------- | ---- |
| profilePicture | File |

**Success Response**

```json
{
  "success": true,
  "message": "Profile picture uploaded successfully.",
  "data": {
    "profilePictureUrl": "https://cdn.aiblog.com/profile/1.jpg"
  }
}
```

---

### 2.5 Delete Profile Picture

**Endpoint:** `DELETE /api/profile/picture`
**Authorization:** User

**Success Response**

```json
{
  "success": true,
  "message": "Profile picture deleted successfully."
}
```

---

### 2.6 Delete Account

**Endpoint:** `DELETE /api/profile`
**Authorization:** User

**Request**

```json
{
  "reason": "No longer using the application."
}
```

**Success Response**

```json
{
  "success": true,
  "message": "Account deleted successfully."
}
```

---

### 2.7 View Public Profile

**Endpoint:** `GET /api/profile/{userId}`
**Authorization:** Public

**Success Response**

```json
{
  "success": true,
  "message": "Profile retrieved successfully.",
  "data": {
    "userId": 12,
    "userName": "johnsmith",
    "displayName": "John Smith",
    "bio": "Tech enthusiast and AI writer.",
    "location": "New York",
    "website": "https://johnsmith.dev",
    "profilePictureUrl": "https://cdn.aiblog.com/profile/12.jpg",
    "followersCount": 125,
    "followingCount": 67,
    "publishedBlogs": 42,
    "isFollowing": false,
    "joinedOn": "2026-01-12T10:30:00Z"
  }
}
```

---

### 2.8 Get User's Published Blogs

**Endpoint:** `GET /api/profile/{userId}/blogs`
**Authorization:** Public

**Success Response**

```json
{
  "success": true,
  "message": "Published blogs retrieved successfully.",
  "data": [
    {
      "blogId": 21,
      "title": "Introduction to AI",
      "excerpt": "Artificial Intelligence is changing the world...",
      "coverImageUrl": "https://cdn.aiblog.com/blogs/21.jpg",
      "category": "Technology",
      "publishedAt": "2026-08-02T12:30:00Z",
      "views": 452,
      "likes": 76,
      "comments": 18
    }
  ]
}
```

---

### 2.9 Follow User

**Endpoint:** `POST /api/profile/{userId}/follow`
**Authorization:** User

**Success Response**

```json
{
  "success": true,
  "message": "User followed successfully."
}
```

---

### 2.10 Unfollow User

**Endpoint:** `DELETE /api/profile/{userId}/follow`
**Authorization:** User

**Success Response**

```json
{
  "success": true,
  "message": "User unfollowed successfully."
}
```

---

### 2.11 Get My Followers

**Endpoint:** `GET /api/profile/followers`
**Authorization:** User

**Success Response**

```json
{
  "success": true,
  "message": "Followers retrieved successfully.",
  "data": [
    {
      "userId": 5,
      "userName": "alex",
      "displayName": "Alex Johnson",
      "profilePictureUrl": "https://cdn.aiblog.com/profile/5.jpg",
      "isFollowing": true
    }
  ]
}
```

---

### 2.12 Get Users I Follow

**Endpoint:** `GET /api/profile/following`
**Authorization:** User

**Success Response**

```json
{
  "success": true,
  "message": "Following list retrieved successfully.",
  "data": [
    {
      "userId": 15,
      "userName": "david",
      "displayName": "David Miller",
      "profilePictureUrl": "https://cdn.aiblog.com/profile/15.jpg"
    }
  ]
}
```

---

### 2.13 Get Another User's Followers

**Endpoint:** `GET /api/profile/{userId}/followers`
**Authorization:** Public

**Success Response**

```json
{
  "success": true,
  "message": "Followers retrieved successfully.",
  "data": []
}
```

---

### 2.14 Get Another User's Following

**Endpoint:** `GET /api/profile/{userId}/following`
**Authorization:** Public

**Success Response**

```json
{
  "success": true,
  "message": "Following list retrieved successfully.",
  "data": []
}
```

---

### 2.15 Get Saved Blogs

**Endpoint:** `GET /api/profile/saved-blogs`
**Authorization:** User

**Success Response**

```json
{
  "success": true,
  "message": "Saved blogs retrieved successfully.",
  "data": []
}
```

---

### 2.16 Get Liked Blogs

**Endpoint:** `GET /api/profile/liked-blogs`
**Authorization:** User

**Success Response**

```json
{
  "success": true,
  "message": "Liked blogs retrieved successfully.",
  "data": []
}
```

---

### 2.17 Get User Statistics

**Endpoint:** `GET /api/profile/{userId}/stats`
**Authorization:** Public

**Success Response**

```json
{
  "success": true,
  "message": "User statistics retrieved successfully.",
  "data": {
    "publishedBlogs": 24,
    "draftBlogs": 6,
    "followers": 185,
    "following": 67,
    "totalViews": 15482,
    "totalLikes": 932,
    "totalComments": 241,
    "totalReposts": 48
  }
}
```

---

### 2.18 Get Activity History

**Endpoint:** `GET /api/profile/activity`
**Authorization:** User

**Success Response**

```json
{
  "success": true,
  "message": "Activity history retrieved successfully.",
  "data": [
    {
      "activityType": "Published",
      "blogId": 15,
      "blogTitle": "Understanding AI",
      "activityDate": "2026-08-03T10:30:00Z"
    }
  ]
}
```

---

### 2.19 Search Users

**Endpoint:** `GET /api/profile/search?query=john`
**Authorization:** User

**Success Response**

```json
{
  "success": true,
  "message": "Users retrieved successfully.",
  "data": [
    {
      "userId": 12,
      "userName": "johnsmith",
      "displayName": "John Smith",
      "profilePictureUrl": "https://cdn.aiblog.com/profile/12.jpg",
      "followers": 125
    }
  ]
}
```

---

### 2.20 Get Suggested Users

**Endpoint:** `GET /api/profile/suggestions`
**Authorization:** User

**Success Response**

```json
{
  "success": true,
  "message": "Suggested users retrieved successfully.",
  "data": [
    {
      "userId": 21,
      "userName": "sarah",
      "displayName": "Sarah Lee",
      "profilePictureUrl": "https://cdn.aiblog.com/profile/21.jpg",
      "mutualFollowers": 6
    }
  ]
}
```

---

### 2.21 Get User Badges

**Endpoint:** `GET /api/profile/{userId}/badges`
**Authorization:** Public

**Success Response**

```json
{
  "success": true,
  "message": "Badges retrieved successfully.",
  "data": [
    {
      "badgeId": 1,
      "name": "Early Adopter",
      "iconUrl": "https://cdn.aiblog.com/badges/early-adopter.png",
      "earnedAt": "2026-02-12T10:30:00Z"
    }
  ]
}
```

---

# 3. AI Blog APIs

### 3.1 Generate Blog

**Endpoint:** `POST /api/blogs/generate`
**Authorization:** User

**Request**

```json
{
  "topic": "Artificial Intelligence",
  "tone": "Professional",
  "audience": "Students",
  "categoryId": 1,
  "wordCount": 1000
}
```

**Success Response**

```json
{
  "success": true,
  "message": "Blog generated successfully.",
  "data": {
    "creditsUsed": 5,
    "remainingCredits": 95,
    "blog": {
      "blogId": 15,
      "title": "Understanding Artificial Intelligence",
      "content": "Generated AI blog content...",
      "category": "Technology",
      "tone": "Professional",
      "audience": "Students",
      "wordCount": 1000,
      "status": "Draft",
      "createdAt": "2026-08-06T10:30:00Z"
    }
  }
}
```

**402 Payment Required**

```json
{
  "success": false,
  "message": "Insufficient credits."
}
```

---

### 3.2 Regenerate Blog

**Endpoint:** `POST /api/blogs/{blogId}/regenerate`
**Authorization:** User

**Success Response**

```json
{
  "success": true,
  "message": "Blog regenerated successfully.",
  "data": {
    "versionId": 4,
    "creditsUsed": 5,
    "remainingCredits": 90,
    "content": "Regenerated blog content..."
  }
}
```

---

### 3.3 Expand Blog

**Endpoint:** `POST /api/blogs/{blogId}/expand`
**Authorization:** User

**Success Response**

```json
{
  "success": true,
  "message": "Blog expanded successfully.",
  "data": {
    "versionId": 5,
    "creditsUsed": 2,
    "remainingCredits": 88,
    "content": "Expanded blog content..."
  }
}
```

---

### 3.4 Shorten Blog

**Endpoint:** `POST /api/blogs/{blogId}/shorten`
**Authorization:** User

**Success Response**

```json
{
  "success": true,
  "message": "Blog shortened successfully.",
  "data": {
    "versionId": 6,
    "creditsUsed": 2,
    "remainingCredits": 86,
    "content": "Shortened blog content..."
  }
}
```

---

### 3.5 Generate Blog Image

**Endpoint:** `POST /api/blogs/{blogId}/generate-image`
**Authorization:** User

**Success Response**

```json
{
  "success": true,
  "message": "Blog image generated successfully.",
  "data": {
    "imageId": 12,
    "creditsUsed": 10,
    "remainingCredits": 76,
    "imageUrl": "https://cdn.aiblog.com/blog-images/12.png"
  }
}
```

---

### 3.6 Get AI History

**Endpoint:** `GET /api/blogs/{blogId}/ai-history`
**Authorization:** User

**Success Response**

```json
{
  "success": true,
  "message": "AI history retrieved successfully.",
  "data": [
    {
      "versionId": 1,
      "versionType": "Original",
      "createdAt": "2026-08-01T10:30:00Z"
    },
    {
      "versionId": 2,
      "versionType": "Regenerated",
      "createdAt": "2026-08-01T10:45:00Z"
    }
  ]
}
```

---

### 3.7 Rewrite Blog

**Endpoint:** `POST /api/blogs/{blogId}/rewrite`
**Authorization:** User

**Request**

```json
{
  "instructions": "Rewrite this blog in a casual tone for beginners."
}
```

**Success Response**

```json
{
  "success": true,
  "message": "Blog rewritten successfully.",
  "data": {
    "versionId": 7,
    "creditsUsed": 5,
    "remainingCredits": 71,
    "content": "Rewritten blog content..."
  }
}
```

---

### 3.8 Translate Blog

**Endpoint:** `POST /api/blogs/{blogId}/translate`
**Authorization:** User

**Request**

```json
{
  "language": "Spanish"
}
```

**Success Response**

```json
{
  "success": true,
  "message": "Blog translated successfully.",
  "data": {
    "versionId": 8,
    "creditsUsed": 5,
    "remainingCredits": 66,
    "language": "Spanish",
    "content": "Contenido traducido..."
  }
}
```

---

### 3.9 Generate SEO Tags

**Endpoint:** `POST /api/blogs/{blogId}/generate-tags`
**Authorization:** User

**Success Response**

```json
{
  "success": true,
  "message": "SEO tags generated successfully.",
  "data": {
    "creditsUsed": 1,
    "remainingCredits": 65,
    "tags": [
      "Artificial Intelligence",
      "Machine Learning",
      "AI",
      "Technology",
      "Automation"
    ]
  }
}
```

---

### 3.10 Generate Blog Summary

**Endpoint:** `POST /api/blogs/{blogId}/generate-summary`
**Authorization:** User

**Success Response**

```json
{
  "success": true,
  "message": "Summary generated successfully.",
  "data": {
    "creditsUsed": 1,
    "remainingCredits": 64,
    "summary": "Artificial Intelligence is transforming industries..."
  }
}
```

---

### 3.11 Get Blog SEO Tags

**Endpoint:** `GET /api/blogs/{blogId}/tags`
**Authorization:** User

**Success Response**

```json
{
  "success": true,
  "message": "SEO tags retrieved successfully.",
  "data": {
    "blogId": 15,
    "tags": [
      "Artificial Intelligence",
      "Machine Learning",
      "Technology",
      "AI",
      "Automation"
    ]
  }
}
```

---

# 4. Blog Management APIs

### 4.1 Get My Blogs

**Endpoint:** `GET /api/blogs`
**Authorization:** User

**Success Response**

```json
{
  "success": true,
  "message": "Blogs retrieved successfully.",
  "data": []
}
```

### 4.2 Get Blog Details

**Endpoint:** `GET /api/blogs/{blogId}`
**Authorization:** User

**Success Response**

```json
{
  "success": true,
  "message": "Blog retrieved successfully.",
  "data": {
    "blogId": 15,
    "title": "Understanding Artificial Intelligence",
    "prompt": "Write a blog about Artificial Intelligence.",
    "content": "Complete blog content...",
    "category": "Technology",
    "tone": "Professional",
    "audience": "Students",
    "wordCount": 1000,
    "status": "Draft",
    "currentVersionId": 3,
    "createdAt": "2026-08-05T10:30:00Z",
    "updatedAt": "2026-08-05T11:20:00Z"
  }
}
```

### 4.3 Get Blog Images

**Endpoint:** `GET /api/blogs/{blogId}/images`
**Authorization:** User

**Success Response**

```json
{
  "success": true,
  "message": "Blog images retrieved successfully.",
  "data": []
}
```

### 4.4 Download Blog PDF

**Endpoint:** `GET /api/blogs/{blogId}/download-pdf`
**Authorization:** User

**Success Response:** `200 OK`

**Content-Type:** `application/pdf`

Returns the generated blog as a PDF file.

### 4.5 Get Blog Versions

**Endpoint:** `GET /api/blogs/{blogId}/versions`
**Authorization:** User

**Success Response**

```json
{
  "success": true,
  "message": "Blog versions retrieved successfully.",
  "data": []
}
```

### 4.6 Delete Blog

**Endpoint:** `DELETE /api/blogs/{blogId}`
**Authorization:** User

**Success Response**

```json
{
  "success": true,
  "message": "Blog deleted successfully."
}
```

### 4.7 Delete Blog Image

**Endpoint:** `DELETE /api/blogs/images/{imageId}`
**Authorization:** User

**Success Response**

```json
{
  "success": true,
  "message": "Blog image deleted successfully."
}
```

### 4.8 Publish Blog

**Endpoint:** `POST /api/blogs/{blogId}/publish`
**Authorization:** User

**Success Response**

```json
{
  "success": true,
  "message": "Blog published successfully.",
  "data": {
    "publishedAt": "2026-08-06T09:30:00Z"
  }
}
```

### 4.9 Unpublish Blog

**Endpoint:** `POST /api/blogs/{blogId}/unpublish`
**Authorization:** User

**Success Response**

```json
{
  "success": true,
  "message": "Blog moved to drafts successfully."
}
```

### 4.10 Update Blog

**Endpoint:** `PUT /api/blogs/{blogId}`
**Authorization:** User

**Request**

```json
{
  "title": "Understanding Artificial Intelligence",
  "content": "Updated blog content...",
  "category": "Technology"
}
```

**Success Response**

```json
{
  "success": true,
  "message": "Blog updated successfully.",
  "data": {
    "updatedAt": "2026-08-06T10:10:00Z"
  }
}
```

### 4.11 Get Draft Blogs

**Endpoint:** `GET /api/blogs/drafts`
**Authorization:** User

**Success Response**

```json
{
  "success": true,
  "message": "Draft blogs retrieved successfully.",
  "data": []
}
```

### 4.12 Get Published Blogs

**Endpoint:** `GET /api/blogs/published`
**Authorization:** User

**Success Response**

```json
{
  "success": true,
  "message": "Published blogs retrieved successfully.",
  "data": []
}
```

**Note:** Copying blog text does not require a backend API because the copy operation is handled by the frontend/browser.

---

# 5. Credit APIs

### 5.1 Get Available Credits

**Endpoint:** `GET /api/credits`
**Authorization:** User

**Success Response — 200 OK**

```json
{
  "success": true,
  "message": "Credits retrieved successfully.",
  "data": {
    "availableCredits": 95
  }
}
```

**401 Unauthorized**

```json
{
  "success": false,
  "message": "Unauthorized."
}
```

---

### 5.2 Get Credit Pricing

**Endpoint:** `GET /api/credits/pricing`
**Authorization:** Public

**Success Response — 200 OK**

```json
{
  "success": true,
  "message": "Credit pricing retrieved successfully.",
  "data": {
    "services": [
      {
        "service": "Generate Blog",
        "credits": 5
      },
      {
        "service": "Regenerate Blog",
        "credits": 5
      },
      {
        "service": "Expand Blog",
        "credits": 2
      },
      {
        "service": "Shorten Blog",
        "credits": 2
      },
      {
        "service": "Generate Image",
        "credits": 10
      },
      {
        "service": "Generate Tags",
        "credits": 1
      },
      {
        "service": "Generate Summary",
        "credits": 1
      }
    ]
  }
}
```

---

# 6. Subscription Plan APIs

### 6.1 Get Subscription Plans

**Endpoint:** `GET /api/plans`
**Authorization:** Public

**Success Response**

```json
{
  "success": true,
  "message": "Subscription plans retrieved successfully.",
  "data": [
    {
      "planId": 1,
      "name": "Free",
      "description": "Free credits on registration.",
      "price": 0,
      "credits": 100,
      "isActive": true
    },
    {
      "planId": 2,
      "name": "Standard",
      "description": "Best for regular users.",
      "price": 50,
      "credits": 100,
      "isActive": true
    },
    {
      "planId": 3,
      "name": "Premium",
      "description": "Suitable for heavy AI usage.",
      "price": 199,
      "credits": 500,
      "isActive": true
    }
  ]
}
```

---

# 7. Payment APIs

### 7.1 Create Payment Intent

**Endpoint:** `POST /api/payments/create-payment-intent`
**Authorization:** User

**Request**

```json
{
  "planId": 2
}
```

**Success Response**

```json
{
  "success": true,
  "message": "Payment intent created successfully.",
  "data": {
    "paymentIntentId": "pi_xxxxxxxxx",
    "clientSecret": "pi_xxxxxxxxx_secret_xxxxxxxxx"
  }
}
```

### 7.2 Confirm Payment

**Endpoint:** `POST /api/payments/confirm`
**Authorization:** User

**Request**

```json
{
  "paymentIntentId": "pi_xxxxxxxxx"
}
```

**Success Response**

```json
{
  "success": true,
  "message": "Payment confirmed successfully.",
  "data": {
    "paymentId": 15,
    "creditsAdded": 100,
    "availableCredits": 225,
    "paymentStatus": "Succeeded"
  }
}
```

### 7.3 Get Payment History

**Endpoint:** `GET /api/payments/history`
**Authorization:** User

**Success Response**

```json
{
  "success": true,
  "message": "Payment history retrieved successfully.",
  "data": []
}
```

### 7.4 Stripe Webhook

**Endpoint:** `POST /api/payments/webhook`
**Authorization:** Stripe

**Request:** Stripe payment event.

**Success Response**

```json
{
  "received": true
}
```

### 7.5 Get Payment Details

**Endpoint:** `GET /api/payments/{paymentId}`
**Authorization:** User

**Success Response**

```json
{
  "success": true,
  "message": "Payment details retrieved successfully.",
  "data": {
    "paymentId": 8,
    "planName": "Premium",
    "amount": 199,
    "creditsPurchased": 500,
    "paymentStatus": "Succeeded",
    "paymentIntentId": "pi_xxxxxxxxx",
    "purchasedAt": "2026-08-06T12:30:00Z"
  }
}
```

### 7.6 Cancel Pending Payment

**Endpoint:** `POST /api/payments/{paymentId}/cancel`
**Authorization:** User

**Success Response**

```json
{
  "success": true,
  "message": "Payment cancelled successfully."
}
```

---

# 8. Feedback APIs

### 8.1 Submit Feedback

**Endpoint:** `POST /api/feedback`
**Authorization:** User

**Request**

```json
{
  "subject": "Great Experience",
  "message": "The AI generated high-quality blogs.",
  "rating": 5,
  "isPublic": true
}
```

**Success Response — 201 Created**

```json
{
  "success": true,
  "message": "Feedback submitted successfully."
}
```

### 8.2 Get My Feedback

**Endpoint:** `GET /api/feedback`
**Authorization:** User

**Success Response**

```json
{
  "success": true,
  "message": "Feedback retrieved successfully.",
  "data": []
}
```

### 8.3 Get Feedback Details

**Endpoint:** `GET /api/feedback/{feedbackId}`
**Authorization:** User

**Success Response**

```json
{
  "success": true,
  "message": "Feedback details retrieved successfully.",
  "data": {
    "feedbackId": 1,
    "subject": "Great Experience",
    "message": "The AI generated high-quality blogs.",
    "rating": 5,
    "status": "Resolved",
    "adminResponse": "Thank you for your feedback.",
    "createdAt": "2026-08-04T12:20:00Z"
  }
}
```

### 8.4 Get Public Feedback

**Endpoint:** `GET /api/feedback/public`
**Authorization:** Public

**Success Response**

```json
{
  "success": true,
  "message": "Public feedback retrieved successfully.",
  "data": [
    {
      "userName": "johnsmith",
      "rating": 5,
      "message": "Excellent application."
    }
  ]
}
```

---

# 9. Issue / Support APIs

### 9.1 Raise Support Issue

**Endpoint:** `POST /api/issues`
**Authorization:** User

**Request**

```json
{
  "subject": "Unable to generate blog",
  "description": "The blog generation request fails even though I have credits."
}
```

**Success Response — 201 Created**

```json
{
  "success": true,
  "message": "Issue raised successfully.",
  "data": {
    "issueId": 12,
    "status": "Open",
    "createdAt": "2026-08-06T12:30:00Z"
  }
}
```

---

### 9.2 Get My Issues

**Endpoint:** `GET /api/issues`
**Authorization:** User

**Success Response**

```json
{
  "success": true,
  "message": "Issues retrieved successfully.",
  "data": [
    {
      "issueId": 12,
      "subject": "Unable to generate blog",
      "status": "Open",
      "createdAt": "2026-08-06T12:30:00Z"
    }
  ]
}
```

---

### 9.3 Get Issue Details

**Endpoint:** `GET /api/issues/{id}`
**Authorization:** User

**Success Response**

```json
{
  "success": true,
  "message": "Issue details retrieved successfully.",
  "data": {
    "issueId": 12,
    "subject": "Unable to generate blog",
    "description": "The blog generation request fails.",
    "status": "InProgress",
    "adminResponse": null,
    "createdAt": "2026-08-06T12:30:00Z"
  }
}
```

---

### 9.4 Update Issue

**Endpoint:** `PUT /api/issues/{id}`
**Authorization:** User

**Request**

```json
{
  "subject": "Unable to generate blog",
  "description": "Updated description of the issue."
}
```

**Success Response**

```json
{
  "success": true,
  "message": "Issue updated successfully."
}
```

**Business Rule:** Users may update an issue only while it has not been resolved or closed.

---

### 9.5 Delete / Withdraw Issue

**Endpoint:** `DELETE /api/issues/{id}`
**Authorization:** User

**Success Response**

```json
{
  "success": true,
  "message": "Issue withdrawn successfully."
}
```

---

### 9.6 Get Issue Status

**Endpoint:** `GET /api/issues/{id}/status`
**Authorization:** User

**Success Response**

```json
{
  "success": true,
  "message": "Issue status retrieved successfully.",
  "data": {
    "issueId": 12,
    "status": "InProgress"
  }
}
```

---

# 10. Admin Dashboard APIs

All Admin Dashboard APIs require **Admin authorization**.

### 10.1 Get Dashboard Statistics

**Endpoint:** `GET /api/admin/dashboard`

**Success Response**

```json
{
  "success": true,
  "message": "Dashboard statistics retrieved successfully.",
  "data": {
    "totalUsers": 1250,
    "activeUsers": 1080,
    "totalBlogs": 5420,
    "publishedBlogs": 4210,
    "totalPayments": 325,
    "totalRevenue": 45890.00,
    "pendingReports": 12
  }
}
```

### 10.2 Get All Users

**Endpoint:** `GET /api/admin/users`

Returns all registered users with administrative information.

### 10.3 Get User Details

**Endpoint:** `GET /api/admin/users/{userId}`

Returns detailed user information.

### 10.4 Get All Blogs

**Endpoint:** `GET /api/admin/blogs`

Returns all blogs for administrative moderation.

### 10.5 Get Blog Details

**Endpoint:** `GET /api/admin/blogs/{blogId}`

Returns detailed information about a specific blog.

### 10.6 Get All Payments

**Endpoint:** `GET /api/admin/payments`

Returns payment records across the platform.

### 10.7 Get Deleted Users

**Endpoint:** `GET /api/admin/deleted-users`

Returns accounts stored in the deleted-account archive.

### 10.8 Get All Feedback

**Endpoint:** `GET /api/admin/feedback`

Returns all submitted feedback.

### 10.9 Resolve Feedback

**Endpoint:** `PUT /api/admin/feedback/{feedbackId}/resolve`

**Request**

```json
{
  "adminResponse": "Thank you for your feedback."
}
```

**Success Response**

```json
{
  "success": true,
  "message": "Feedback resolved successfully."
}
```

### 10.10 Get All Issues

**Endpoint:** `GET /api/admin/issues`

Returns all support issues.

### 10.11 Resolve Issue

**Endpoint:** `PUT /api/admin/issues/{issueId}/resolve`

**Request**

```json
{
  "adminResponse": "The issue has been resolved."
}
```

**Success Response**

```json
{
  "success": true,
  "message": "Issue resolved successfully."
}
```

### 10.12 Get User Payment History

**Endpoint:** `GET /api/admin/users/{userId}/payments`

Returns payment history for a specific user.

### 10.13 Create Subscription Plan

**Endpoint:** `POST /api/admin/plans`

**Request**

```json
{
  "name": "Premium",
  "description": "Suitable for heavy AI usage.",
  "price": 199,
  "credits": 500
}
```

**Success Response**

```json
{
  "success": true,
  "message": "Subscription plan created successfully.",
  "data": {
    "planId": 3
  }
}
```

### 10.14 Update Subscription Plan

**Endpoint:** `PUT /api/admin/plans/{planId}`

**Request**

```json
{
  "name": "Premium",
  "description": "Updated premium plan.",
  "price": 199,
  "credits": 500,
  "isActive": true
}
```

### 10.15 Delete Subscription Plan

**Endpoint:** `DELETE /api/admin/plans/{planId}`

**Success Response**

```json
{
  "success": true,
  "message": "Subscription plan deleted successfully."
}
```

### 10.16 Block User

**Endpoint:** `PUT /api/admin/users/{userId}/block`

**Success Response**

```json
{
  "success": true,
  "message": "User account blocked successfully."
}
```

### 10.17 Unblock User

**Endpoint:** `PUT /api/admin/users/{userId}/unblock`

**Success Response**

```json
{
  "success": true,
  "message": "User account unblocked successfully."
}
```

### 10.18 Remove Blog

**Endpoint:** `DELETE /api/admin/blogs/{blogId}`

**Success Response**

```json
{
  "success": true,
  "message": "Blog removed successfully."
}
```

### 10.19 Get Reported Blogs

**Endpoint:** `GET /api/admin/reported-blogs`

Returns reported blogs pending moderation.

### 10.20 Resolve Blog Report

**Endpoint:** `PUT /api/admin/reported-blogs/{reportId}/resolve`

**Request**

```json
{
  "reportStatus": "Resolved"
}
```

**Success Response**

```json
{
  "success": true,
  "message": "Blog report resolved successfully."
}
```

### 10.21 Get Platform Statistics

**Endpoint:** `GET /api/admin/statistics`

Returns detailed analytics including users, blogs, views, likes, comments, reposts, payments and revenue.

---

# 11. Public Feed APIs

### 11.1 Get Public Feed

**Endpoint:** `GET /api/feed`
**Authorization:** Public

**Success Response**

```json
{
  "success": true,
  "message": "Public feed retrieved successfully.",
  "data": [
    {
      "blogId": 21,
      "title": "Introduction to AI",
      "excerpt": "Artificial Intelligence is changing the world...",
      "author": {
        "userId": 12,
        "userName": "johnsmith"
      },
      "category": "Technology",
      "coverImageUrl": "https://cdn.aiblog.com/blogs/21.jpg",
      "publishedAt": "2026-08-06T10:30:00Z",
      "views": 452,
      "likes": 76,
      "comments": 18
    }
  ]
}
```

### 11.2 Get Trending Blogs

**Endpoint:** `GET /api/feed/trending`
**Authorization:** Public

Returns published blogs ranked using engagement and recent activity.

### 11.3 Get Following Feed

**Endpoint:** `GET /api/feed/following`
**Authorization:** User

Returns published blogs from users followed by the logged-in user.

### 11.4 Get Blogs by Category

**Endpoint:** `GET /api/feed/category/{category}`
**Authorization:** Public

Returns published blogs belonging to the specified category.

### 11.5 Search Published Blogs

**Endpoint:** `GET /api/feed/search?query=artificial+intelligence`
**Authorization:** Public

Searches published blogs by title, tags and author.

### 11.6 View Published Blog

**Endpoint:** `GET /api/feed/{blogId}`
**Authorization:** Public

Returns the complete publicly visible blog.

---

# 12. Blog Interaction APIs

### 12.1 Like Blog

**Endpoint:** `POST /api/blogs/{blogId}/like`
**Authorization:** User

**Success Response**

```json
{
  "success": true,
  "message": "Blog liked successfully."
}
```

### 12.2 Unlike Blog

**Endpoint:** `DELETE /api/blogs/{blogId}/like`
**Authorization:** User

**Success Response**

```json
{
  "success": true,
  "message": "Blog unliked successfully."
}
```

### 12.3 Get Blog Likes

**Endpoint:** `GET /api/blogs/{blogId}/likes`
**Authorization:** Public

Returns users who liked the blog.

### 12.4 Add Comment

**Endpoint:** `POST /api/blogs/{blogId}/comments`
**Authorization:** User

**Request**

```json
{
  "parentCommentId": null,
  "content": "Great article!"
}
```

**Success Response**

```json
{
  "success": true,
  "message": "Comment added successfully.",
  "data": {
    "commentId": 101
  }
}
```

### 12.5 Get Comments

**Endpoint:** `GET /api/blogs/{blogId}/comments`
**Authorization:** Public

Returns comments and their replies.

### 12.6 Edit Comment

**Endpoint:** `PUT /api/comments/{commentId}`
**Authorization:** User

**Request**

```json
{
  "content": "Updated comment."
}
```

### 12.7 Delete Comment

**Endpoint:** `DELETE /api/comments/{commentId}`
**Authorization:** User

**Success Response**

```json
{
  "success": true,
  "message": "Comment deleted successfully."
}
```

### 12.8 Save Blog

**Endpoint:** `POST /api/blogs/{blogId}/save`
**Authorization:** User

**Success Response**

```json
{
  "success": true,
  "message": "Blog saved successfully."
}
```

### 12.9 Remove Saved Blog

**Endpoint:** `DELETE /api/blogs/{blogId}/save`
**Authorization:** User

**Success Response**

```json
{
  "success": true,
  "message": "Blog removed from saved blogs."
}
```

### 12.10 Repost Blog

**Endpoint:** `POST /api/blogs/{blogId}/repost`
**Authorization:** User

**Request**

```json
{
  "caption": "Worth reading!"
}
```

**Success Response**

```json
{
  "success": true,
  "message": "Blog reposted successfully."
}
```

### 12.11 Remove Repost

**Endpoint:** `DELETE /api/blogs/{blogId}/repost`
**Authorization:** User

**Success Response**

```json
{
  "success": true,
  "message": "Repost removed successfully."
}
```

### 12.12 Report Blog

**Endpoint:** `POST /api/blogs/{blogId}/report`
**Authorization:** User

**Request**

```json
{
  "reason": "InappropriateContent",
  "description": "This blog contains inappropriate content."
}
```

**Success Response**

```json
{
  "success": true,
  "message": "Blog reported successfully."
}
```

### 12.13 Like Comment

**Endpoint:** `POST /api/comments/{commentId}/like`
**Authorization:** User

**Success Response**

```json
{
  "success": true,
  "message": "Comment liked successfully."
}
```

### 12.14 Unlike Comment

**Endpoint:** `DELETE /api/comments/{commentId}/like`
**Authorization:** User

**Success Response**

```json
{
  "success": true,
  "message": "Comment unliked successfully."
}
```

---

# 13. Notification APIs

### 13.1 Get Notifications

**Endpoint:** `GET /api/notifications`
**Authorization:** User

**Success Response**

```json
{
  "success": true,
  "message": "Notifications retrieved successfully.",
  "data": [
    {
      "notificationId": 1,
      "notificationType": "Like",
      "message": "Alex liked your blog.",
      "senderUserId": 5,
      "blogId": 21,
      "commentId": null,
      "isRead": false,
      "createdAt": "2026-08-06T14:30:00Z"
    }
  ]
}
```

### 13.2 Mark Notification as Read

**Endpoint:** `PUT /api/notifications/{notificationId}/read`
**Authorization:** User

**Success Response**

```json
{
  "success": true,
  "message": "Notification marked as read."
}
```

### 13.3 Mark All Notifications as Read

**Endpoint:** `PUT /api/notifications/read-all`
**Authorization:** User

**Success Response**

```json
{
  "success": true,
  "message": "All notifications marked as read."
}
```

---

# 14. Categories APIs

### 14.1 Get Categories

**Endpoint:** `GET /api/categories`
**Authorization:** Public

**Success Response**

```json
{
  "success": true,
  "message": "Categories retrieved successfully.",
  "data": [
    {
      "categoryId": 1,
      "name": "Technology",
      "description": "Technology and software related blogs.",
      "icon": "https://cdn.aiblog.com/categories/technology.png"
    },
    {
      "categoryId": 2,
      "name": "Education",
      "description": "Educational and learning content.",
      "icon": "https://cdn.aiblog.com/categories/education.png"
    }
  ]
}
```

---

# 15. Standard Error Responses

The API uses a consistent error response structure.

### 400 Bad Request

```json
{
  "success": false,
  "message": "Invalid request.",
  "errors": {}
}
```

### 401 Unauthorized

```json
{
  "success": false,
  "message": "Unauthorized."
}
```

### 403 Forbidden

```json
{
  "success": false,
  "message": "You do not have permission to perform this action."
}
```

### 404 Not Found

```json
{
  "success": false,
  "message": "Resource not found."
}
```

### 409 Conflict

```json
{
  "success": false,
  "message": "Resource already exists."
}
```

### 402 Payment Required

```json
{
  "success": false,
  "message": "Insufficient credits."
}
```

### 500 Internal Server Error

```json
{
  "success": false,
  "message": "An unexpected error occurred."
}
```

---

# 16. Authorization Levels

| Authorization | Description                                                |
| ------------- | ---------------------------------------------------------- |
| Public        | Endpoint can be accessed without authentication            |
| User          | Endpoint requires a valid JWT access token                 |
| Admin         | Endpoint requires a valid JWT access token with Admin role |
| Stripe        | Endpoint is accessed by Stripe webhook events              |

---

# 17. API Summary

| Module             | Number of APIs |
| ------------------ | -------------: |
| Authentication     |              4 |
| User Profile       |             21 |
| AI Blog            |             11 |
| Blog Management    |             12 |
| Credits            |              2 |
| Subscription Plans |              1 |
| Payments           |              6 |
| Feedback           |              4 |
| Issues             |              6 |
| Admin Dashboard    |             21 |
| Public Feed        |              6 |
| Blog Interaction   |             14 |
| Notifications      |              3 |
| Categories         |              1 |
| **Total**          |   **112 APIs** |

---

# 18. API Design Notes

* All authenticated endpoints use JWT Bearer authentication.
* Refresh tokens are stored and can be revoked during logout.
* Users receive **100 free credits** upon registration.
* AI operations deduct credits according to the configured credit pricing.
* A user's available credits cannot become negative.
* AI operations are blocked when the user has insufficient credits.
* Users can manage only their own blogs, comments, bookmarks, likes, reposts and profile information.
* Public users can view published blogs and public profiles.
* Only published blogs are available through the public feed.
* Only administrators can access administrative endpoints.
* Blog reports are reviewed and resolved by administrators.
* Stripe webhooks are used to receive payment events and update payment status.
* Deleted user accounts are retained in the `DeletedAccounts` archive without a foreign-key relationship to `Users`.
* Copying blog content is handled by the frontend/browser and therefore does not require a backend API.
* AI-generated operations create corresponding blog versions where applicable.
* Notifications are generated for relevant social interactions such as likes, comments, follows and reposts.
